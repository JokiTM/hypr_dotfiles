alias ll='ls -lA'
alias l='ls -l'
alias chx='chmod +x'

# Shortcuts for config files
alias cf='nvim ~/.config/fish/config.fish'
alias cz='nvim ~/.config/zsh/.zshrc'
alias cza='nvim ~/.config/zsh/alias'
alias cr='source ~/.config/zsh/.zshrc'
alias cn='nvim'
alias cnp='cd ~/.config/nvim/lua/plugins/ && nvim ~/.config/nvim/lua/config/plugins/'

#hypr
alias ch='nvim ~/.config/hypr/hyprland.conf'
alias chk='nvim ~/.config/hypr/conf/keybinds.conf'
alias che='nvim ~/.config/hypr/conf/execs.conf'
alias chm='nvim ~/.config/hypr/conf/monitors.conf'
alias reloadLock="hyprctl --instance 0 'dispatch exec hyprlock'"

alias cy='nvim ~/.config/yazi/yazi.toml'
alias cw='nvim ~/.config/waybar/config.jsonc'
alias cwd='nvim ~/.config/waybar/default-modules.json'
alias cdd='cd ~/.local/share/dots/'

alias mss='sudo mount -t nfs4 mathisserver.local:/  ~/Videos/Serien/mathisserver'

alias got='y ~/Videos/Serien/Game\ of\ Thrones/'

alias bimp='beet imp ~/Piracy/Nicotine/'
alias ms='mount ~/sansa'

alias savepic='cp /tmp/catgirl "/home/mathis/Pictures/Wallpapers/sexyanime/$(cat /dev/urandom | tr -cd 'a-f0-9' | head -c 12).$(magick identify -format "%m\n" /tmp/catgirl)"' 

#git
alias gs='git status'
alias gd='git diff'
alias ga='git add .'
alias gc='git commit -m '
alias gp='git push'
alias gcm='git checkout main'

alias z='zathura'

# Yazi shell wrapper
function y() {
	local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
	command yazi "$@" --cwd-file="$tmp"
	IFS= read -r -d '' cwd < "$tmp"
	[ "$cwd" != "$PWD" ] && [ -d "$cwd" ] && builtin cd -- "$cwd"
	rm -f -- "$tmp"
}
